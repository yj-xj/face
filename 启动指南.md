# 人脸替换应用 - 启动指南

## 前后端启动方法

### 方法一：手动启动（推荐用于开发）

#### 1. 启动 Django 后端

打开第一个命令行窗口（终端）：

```bash
cd E:\face\backend-django
E:\anaconda\envs\face_swap\python.exe manage.py runserver 0.0.0.0:8000
```

**后端服务地址：**
- API 服务：http://localhost:8000/api/v1/
- API 文档：http://localhost:8000/api/docs/
- 管理后台：http://localhost:8000/admin/

#### 2. 启动 PyQt 前端

打开第二个命令行窗口（终端）：

```bash
cd E:\face\frontend
E:\anaconda\envs\face_swap\python.exe main.py
```

**界面会自动弹出！**

---

### 方法二：使用批处理脚本（Windows）

创建以下两个批处理文件：

#### `start_backend.bat`
```batch
@echo off
cd /d E:\face\backend-django
echo Starting Django Backend...
E:\anaconda\envs\face_swap\python.exe manage.py runserver 0.0.0.0:8000
pause
```

#### `start_frontend.bat`
```batch
@echo off
cd /d E:\face\frontend
echo Starting PyQt Frontend...
E:\anaconda\envs\face_swap\python.exe main.py
pause
```

**使用方法：**
1. 双击 `start_backend.bat` 启动后端
2. 双击 `start_frontend.bat` 启动前端（界面会弹出）

---

## 当前运行状态

### ✅ Django 后端
- **状态：** 正在运行
- **地址：** http://0.0.0.0:8000/
- **进程 ID：** b85fd85
- **启动时间：** 19:03:12

### ✅ PyQt 前端
- **状态：** 正在运行
- **进程 ID：** b6ccb49
- **启动时间：** 19:30:34

前端界面已经启动！如果没有看到窗口，请检查：
1. 任务栏是否有 "人脸替换应用" 图标
2. 窗口可能被其他窗口遮挡
3. 尝试按 Alt+Tab 切换窗口

---

## 功能说明

### 前端界面功能
1. **视频模式** - 上传视频进行人脸替换
2. **摄像头模式** - 实时摄像头人脸替换
3. **上传功能** - 将视频和图片上传到后端数据库
4. **播放控制** - 支持变速播放（0.25x - 2.0x）

### 后端 API 功能
- `/api/v1/images/` - 人脸图片管理
- `/api/v1/videos/` - 输入视频管理
- `/api/v1/outputs/` - 输出视频管理
- `/api/v1/tasks/` - 处理任务管理

---

## 常见问题

### Q: 为什么看不到界面？
A: 前端应用已经在后台运行。请检查任务栏或按 Alt+Tab 查找窗口。

### Q: 如何停止服务？
A: 在对应的命令行窗口按 `Ctrl+C` 即可停止服务。

### Q: 后端数据库使用的是什么？
A: 开发环境默认使用 SQLite（无需安装数据库），可以切换到 MySQL。

### Q: 出现 OpenCV 错误怎么办？
A: 颜色平滑错误是正常的，不影响主要功能。如果影响使用，可以在设置中关闭颜色校正。

---

## 技术架构

### 前端 (PyQt5)
- 目录：`E:\face\frontend\`
- 入口：`main.py`
- 配置：`config.py`

### 后端 (Django)
- 目录：`E:\face\backend-django\`
- 入口：`manage.py`
- API：`/api/v1/`

---

## 下一步

1. ✅ 前后端已成功启动
2. 打开浏览器访问 http://localhost:8000/api/docs/ 查看 API 文档
3. 使用 PyQt 界面进行人脸替换操作
4. 视频和图片会自动上传到后端数据库

**祝您使用愉快！**

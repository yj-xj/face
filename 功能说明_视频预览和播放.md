# 视频预览和播放功能说明

## ✅ 已实现的功能

### 1. 视频缩略图预览
- **自动生成**：每个视频自动提取第一帧作为缩略图
- **尺寸**：200x150像素，保持宽高比
- **显示位置**：视频列表中每个视频项的左侧
- **加载方式**：
  - 从数据库加载时自动生成
  - 从本地文件加载时自动生成

### 2. 双击播放视频
- **操作方式**：双击视频列表中的任意视频项
- **播放器**：使用OpenCV播放器
- **自动处理**：
  - 自动停止当前正在播放的视频
  - 清空视频标签
  - 显示"正在加载视频..."提示
  - 加载完成后自动播放

### 3. 视频列表样式
- **图标大小**：200x150像素
- **项目大小**：220x180像素
- **视觉效果**：
  - 悬停时高亮显示
  - 选中时紫色渐变背景
  - 暗色科技感主题

## 🎬 使用方法

### 查看视频预览
1. 启动应���后，视频列表会自动显示所有视频
2. 每个视频左侧显示第一帧的缩略图
3. 右侧显示视频文件名

### 播放视频
- **方法1**：双击视频列表中的任意视频
- **方法2**：单击选择视频，然后点击播放按钮

### 上传新视频
1. 点击"[上传] 添加视频"按钮
2. 选择本地视频文件
3. 系统会：
   - 保存本地路径到数据库
   - 自动生成缩略图
   - 刷新视频列表

## 📋 技术实现

### 缩略图生成代码
```python
# 生成视频缩略图
cap = cv2.VideoCapture(local_path)
if cap.isOpened():
    ret, frame = cap.read()
    if ret:
        # 转换为QPixmap并设置为图标
        frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
        h, w, c = frame.shape
        q_img = QImage(frame.data, w, h, w * c, QImage.Format_RGB888)
        pixmap = QPixmap.fromImage(q_img)
        pixmap = pixmap.scaled(200, 150, Qt.KeepAspectRatio, Qt.SmoothTransformation)
        item.setIcon(QIcon(pixmap))
    cap.release()
```

### 双击播放代码
```python
def playVideoFromList(self, item):
    """双击视频列表项播放视频"""
    video_path = item.data(Qt.UserRole)

    if not video_path or not os.path.exists(video_path):
        QMessageBox.warning(self, "警告", f"视频文件不存在: {video_path}")
        return

    # 停止当前播放
    self.stopPlayback()

    # 清空视频标签并重置
    self.cv_video_label.clear()
    self.cv_video_label.setText("<font color='#AAAAAA'>正在加载视频...</font>")

    # 保存当前视频路径
    self.current_video_path = video_path

    # 使用OpenCV播放视频
    self.playWithOpenCV()
```

### 事件连接
```python
self.video_list.itemClicked.connect(self.selectVideoFile)
self.video_list.itemDoubleClicked.connect(self.playVideoFromList)
```

## 🎯 功能特性

1. **快速预览**：无需播放即可快速查看视频内容
2. **即时播放**：双击即可播放，无需额外操作
3. **自动刷新**：上传新视频后自动更新列表
4. **错误处理**：
   - 文件不存在时显示警告
   - 缩略图生成失败时使用默认图标
   - 播放失败时显示错误信息

## 📊 日志输出

正常启动时会看到：
```
[回调] onVideosLoaded 被调用，收到 X 个视频
[缩略图] Video_1.mp4
[缩略图] Video_2.mp4
...
[完成] 共加载 X 个视频
```

双击播放时会看到：
```
[双击播放] Video_1.mp4
正在使用OpenCV播放视频: C:/path/to/Video_1.mp4
视频信息: 720x1280, 30.00fps, XXX帧
```

## 🔧 故障排除

### 缩略图不显示
- 检查视频文件是否存在
- 确保视频格式支持（.mp4, .avi, .mov, .mkv）
- 查看控制台日志确认是否生成成功

### 双击无响应
- 确保视频文件存在且可访问
- 检查OpenCV是否正常工作
- 查看控制台错误信息

### 播放卡顿
- 检查视频分辨率是否过高
- 尝试使用较小的缩略图尺寸
- 确保系统资源充足

## 🎨 界面预览

```
┌─────────────────────────────────────┐
│ [视频缩略图] Video_1.mp4            │
│ ┌─────────┐                         │
│ │         │                         │
│ │  预览图 │                         │
│ │         │                         │
│ └─────────┘                         │
└─────────────────────────────────────┘
```

## ✨ 未来改进

- 添加异步缩略图加载（避免卡顿）
- 支持拖放上传视频到列表
- 添加视频时长显示
- 支持多选批量操作
- 添加视频信息悬浮提示

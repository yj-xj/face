# 人脸替换应用 - 数据库功能说明

## 已完成的修改

### 1. 数据库模型更新
- ✅ 添加了 `local_path` 字段到 `FaceImage` 和 `InputVideo` 模型
- ✅ 现在保存本地文件路径而不是上传文件内容

### 2. 后端API更新
- ✅ 修改了上传接口，接收 JSON 数据（包含 `local_path`）
- ✅ 添加了 `JSONParser` 支持
- ✅ 修复了API URL路径问题

### 3. 前端更新
- ✅ 优先从数据库加载文件
- ✅ 上传时保存本地路径
- ✅ 修复了 QThread 销毁问题
- ✅ 添加了线程管理（upload_threads 和 load_threads）

### 4. 文件导入
- ✅ 成功导入了 8 个人脸图片
- ✅ 成功导入了 1 个输入视频
- ✅ 创建了导入工具 `import_local_files.py`

## 使用方法

### 启动后端
```bash
cd E:/face/backend-django
python manage.py runserver 0.0.0.0:8000
```

### 启动��端
```bash
cd E:/face/frontend
python main.py
```

### 上传新文件
1. **上传图片**：点击"[上传] 添加图片"按钮
2. **上传视频**：点击"[上传] 添加视频"按钮
3. 选择本地文件
4. 文件的本地路径会自动保存到数据库

### 数据库中的数据
- **图片**: 18 个（其中 8 个包含本地路径）
- **视频**: 9 个（其中 3 个包含本地路径）

### 查看本地路径的文件
数据库中包含本地路径的文件：

**图片** (e:/face/data/input_faces/):
- 1656516654706.jpg
- 5141834b9d78094_size247_w600_h808.jpg
- 9276F983BFCBDE372DC9844B9E8AE9CA.jpg
- 989356cb736f44ada4dc52f0be207790.jpg
- a4e2-iwpcxks5284648.jpg
- image.png
- OIP-C.jpg
- u=818738938,1048924218&fm=253&fmt=auto&app=138&f=JPEG.jpg

**视频** (e:/face/data/input_videos/):
- Video_1728365311307.mp4

这些文件现在应该能在前端界面中看到！

## 重新导入文件（如果需要）

如果需要重新导入本地文件到数据库：

```bash
cd E:/face/backend-django
python import_local_files.py
```

## 测试数据库内容

```bash
python E:/face/test_database_content.py
```

## 注意事项

1. **本地文件必须存在**：数据库中保存的是本地路径，确保文件路径对应的文件存在
2. **后端必须运行**：前端需要后端API支持
3. **线程安全**：所有上传和加载线程现在都被正确管理

## 完整工作流程

1. **启动后端**：`python manage.py runserver`
2. **启动前端**：`python main.py`
3. **查看文件**：前端会自动从数据库加载并显示有本地路径的文件
4. **选择文件**：从列表中选择要处理的视频和人脸图片
5. **开始处理**：点击"开始处理"进行换脸
6. **保存结果**：处理完成后会自动保存到数据库和本地文件夹

## 故障排除

### 问题：前端看不到图片和视频
**解决方案**：
1. 确保后端正在运行
2. 运行导入脚本：`python import_local_files.py`
3. 检查文件路径是否正确

### 问题：QThread 销毁错误
**解决方案**：已修复，确保使用最新代码

### 问题：无法连接到服务器
**解决方案**：
1. 检查后端是否运行
2. 检查端口 8000 是否被占用
3. 查看后端日志

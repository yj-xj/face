# 人脸替换系统 - 使用说明

## 系统架构

- **后端**: Django REST Framework (http://localhost:8000)
- **前端**: PyQt5 GUI应用
- **数据库**: SQLite3 (backend-django/db.sqlite3)

## 修复内容

### ✅ 已修复的问题

1. **后端URL路由** - 从 `/api/v1/` 改为 `/api/`
2. **上传功能** - 修复了图片、视频、输出视频的上传接口
3. **QThread销毁问题** - 修复了前端上传线程引用问题
4. **暗色科技感主题** - 应用全新的暗色UI主题

## 启动步骤

### 1. 启动后端

```bash
cd E:\face\backend-django
E:/anaconda/envs/face_swap/python.exe manage.py runserver 0.0.0.0:8000
```

或使用批处理文件：
```bash
E:\face\start_backend.bat
```

**确认后端启动成功**：
- 看到 "Starting development server at http://0.0.0.0:8000/"
- 浏览器访问 http://localhost:8000/api/images/ 应该返回JSON数据

### 2. 启动前端

```bash
cd E:\face\frontend
E:/anaconda/envs/face_swap/python.exe main.py
```

或使用批处理文件：
```bash
E:\face\start_frontend_debug.bat
```

**确认前端启动成功**：
- 看到暗色科技感界面
- 能看到人脸图片列表
- 能看到视频列表

## API测试

运行测试脚本验证后端功能：

```bash
cd E:\face
E:/anaconda/envs/face_swap/python.exe test_upload_api.py
```

**测试内容**：
- 获取图片列表
- 上传图片
- 上传视频

## 功能使用

### 上传图片

1. 点击界面上方的"上传图片"按钮
2. 选择图片文件（支持 jpg, jpeg, png, bmp）
3. 等待上传完成
4. 上传成功后会自动刷新图片列表

### 上传视频

1. 在视频模式点击"[上传] 添加视频"按钮
2. 选择视频文件（支持 mp4, avi, mov, mkv, flv, wmv）
3. 等待上传完成
4. 上传成功后会自动刷新视频列表

### 处理视频

1. 选择一个人脸图片
2. 选择一个视频文件
3. 点击"开始处理"按钮
4. 等待处理完成
5. 处理完成后，输出视频会自动保存到数据库

## 数据库查看

使用Navicat查看数据：

1. 打开Navicat
2. 连接 → SQLite
3. 数据库文件: `E:\face\backend-django\db.sqlite3`

**数据表**：
- `face_images` - 人脸图片（9条记录）
- `input_videos` - 输入视频（1条记录）
- `output_videos` - 输出视频（5条记录）

## API端点

### 图片接口
- `GET /api/images/` - 获取图片列表
- `POST /api/images/` - 上传图片
- `GET /api/images/{id}/` - 获取图片详情
- `DELETE /api/images/{id}/` - 删除图片

### 视频接口
- `GET /api/videos/` - 获取视频列表
- `POST /api/videos/` - 上传视频
- `GET /api/videos/{id}/` - 获取视频详情
- `DELETE /api/videos/{id}/` - 删除视频

### 输出视频接口
- `GET /api/outputs/` - 获取输出视频列表
- `POST /api/outputs/` - 创建输出视频
- `GET /api/outputs/{id}/` - 获取输出视频详情

## 界面主题

### 暗色科技感主题特点
- 深色渐变背景 (#1a1d24 → #0d0f12)
- 蓝色科技感高亮 (#4a9eff)
- 半透明组件
- 圆角边框
- 悬停动画效果

## 故障排查

### 问题1: 后端启动失败
**检查**：
- 端口8000是否被占用
- Python环境是否正确

**解决**：
```bash
# 查看端口占用
netstat -ano | findstr :8000

# 杀死进程
taskkill /PID {进程ID} /F
```

### 问题2: 上传失败
**检查**：
- 后端是否正在运行
- 文件大小是否超过500MB
- 文件格式是否支持

**解决**：
- 确保后端已启动
- 检查文件大小和格式
- 查看浏览器控制台错误信息

### 问题3: 前端无法启动
**检查**：
- PyQt5是否安装
- Python环境是否正确

**解决**：
```bash
E:/anaconda/envs/face_swap/python.exe -m pip install PyQt5
```

## 开发日志

### 2025-12-31 修复记录

1. **URL路由问题** (face_swap/urls.py)
   - 从 `path('api/v1/', ...)` 改为 `path('api/', ...)`

2. **上传接口问题** (api/views.py)
   - 修复了图片上传的create方法
   - 修复了视频上传的create方法
   - 修复了输出视频的create方法
   - 改为直接使用model创建对象，避免serializer验证file字段

3. **前端回调问题** (face_swap_ui_enhanced.py)
   - 添加了 `onImageUploadSuccess()` 方法
   - 添加了 `onFaceImageUploadSuccess()` 方法
   - 修复了上传线程的引用保存

4. **UI主题** (face_swap_ui_enhanced.py)
   - 应用全新的暗色科技感主题
   - 使用渐变背景和半透明效果
   - 添加悬停和点击动画

## 当前状态

### ✅ 后端
- Django服务正常运行
- API接口测试通过
- 上传功能正常
- 数据库存储正常

### ✅ 前端
- 暗色主题已应用
- 上传回调已修复
- 数据库集成完成

### ✅ 数据库
- 图片: 9条记录
- 视频: 1条记录
- 输出视频: 5条记录

## 下一步

请启动前端并测试完整功能：

```bash
# 终端1 - 后端（已在运行）
cd E:\face\backend-django
E:/anaconda/envs/face_swap/python.exe manage.py runserver 0.0.0.0:8000

# 终端2 - 前端
cd E:\face\frontend
E:/anaconda/envs/face_swap/python.exe main.py
```

如有任何问题，请提供详细的错误信息。
